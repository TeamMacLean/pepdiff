# Check if values are useable (non-NA and non-NaN)

Check if values are useable (non-NA and non-NaN)

## Usage

``` r
is_useable(x)
```

## Arguments

- x:

  Vector of values

## Value

Logical vector indicating positions of useable values
