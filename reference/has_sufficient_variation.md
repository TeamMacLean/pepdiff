# Check if a model has enough variation to fit

Check if a model has enough variation to fit

## Usage

``` r
has_sufficient_variation(values)
```

## Arguments

- values:

  Numeric vector of values

## Value

Logical
