# Validate a pepdiff_data object

Checks that all required components are present and correctly
structured.

## Usage

``` r
validate_pepdiff_data(x)
```

## Arguments

- x:

  A pepdiff_data object to validate

## Value

The validated object (invisibly), or throws an error
